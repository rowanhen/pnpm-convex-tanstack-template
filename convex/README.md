# Convex

This folder is the shared backend for the template workspace.

- add your schema in `schema.ts`
- add queries / mutations in `messages.ts` or split by domain
- run `pnpm env:sync` to push selected runtime env vars to Convex
